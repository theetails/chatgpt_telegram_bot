apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "telegram-bot.fullname" . }}-test-connection"
  labels:
    {{- include "telegram-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "telegram-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
